<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.jredu.myprojects.fragment.Fragment_square">

    <LinearLayout
        android:id="@+id/squareLayout"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="@color/colorhuishallow">

        <EditText
            android:id="@+id/squre_edit"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="5dp"
            android:background="@android:drawable/edit_text"
            android:drawableLeft="@drawable/icon_search"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:hint="搜索"
            android:inputType="text"
            android:padding="14dp" />

    </LinearLayout>

    <ScrollView
        android:id="@+id/fragment_square_scrollview"
        android:layout_width="match_parent"
        android:layout_height="510dp"
        android:layout_below="@+id/squareLayout"
        android:scrollbars="none">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">
            <ListView
                android:id="@+id/square_listView"
                android:layout_width="match_parent"
                android:layout_height="1000dp">

            </ListView>
        </LinearLayout>
    </ScrollView>

</RelativeLayout>
